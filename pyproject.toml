[tool.ruff]
line-length = 120
target-version = "py311"
extend-exclude = [
  "data",
  ".venv",
]

[tool.ruff.lint]
select = ["E9", "F63", "F7", "F82"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
